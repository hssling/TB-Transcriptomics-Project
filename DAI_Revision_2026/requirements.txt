# Environment for the GSE89403 treatment-outcome analysis.
#
# The scikit-learn and XGBoost versions below are those the frozen models in
# models/ were built under, recorded also in models/MANIFEST.json. Later
# versions will generally reproduce the analysis from scratch, but unpickling
# the frozen models is only guaranteed against these.
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn==1.8.0
xgboost==3.1.2
shap>=0.44
statsmodels>=0.14
matplotlib>=3.8
networkx>=3.0
gseapy>=1.1
python-docx>=1.1
pyarrow>=15
joblib>=1.3
